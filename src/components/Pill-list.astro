---
import Pill from "./Pill.astro"

const {tech, classes} = Astro.props
---

<ul class=`flex flex-wrap gap-2 mt-4 ${classes? classes : ''}`>
    {tech ? tech.map(
        (techText: string)=> <Pill>
            {techText}
        </Pill>
    ):null}
</ul>